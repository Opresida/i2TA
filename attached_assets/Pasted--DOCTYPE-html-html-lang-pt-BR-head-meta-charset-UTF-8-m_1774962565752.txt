<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FAQ Glowing Border Effect</title>
    <!-- Iconify para a setinha do FAQ -->
    <script src="https://code.iconify.design/iconify-icon/1.0.7/iconify-icon.min.js"></script>
    
    <style>
        /* Estilos base para centralizar na tela */
        body {
            background-color: #0A0A0A;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            font-family: 'Inter', sans-serif;
            color: #fff;
        }

        .faq-wrapper {
            width: 100%;
            max-width: 600px;
            padding: 20px;
        }

        /* =========================================
           CSS EXTRAÍDO DO EFEITO FAQ GLOWING BORDER
           ========================================= */

        @keyframes borderTurn {
            0% { transform: translate(-50%, -50%) rotate(0deg); }
            100% { transform: translate(-50%, -50%) rotate(360deg); }
        }

        /* 1. O container principal do FAQ */
        .faq-item {
            position: relative;
            background: rgba(255, 255, 255, 0.02);
            border-radius: 12px;
            transition: all 0.4s cubic-bezier(0.2, 0.8, 0.2, 1);
            overflow: hidden; /* Corta o gradiente que sobrar para fora */
            isolation: isolate;
            margin-bottom: 16px;
        }

        /* Efeito ao passar o mouse ou ativar */
        .faq-item:hover {
            background: rgba(255, 255, 255, 0.04);
            transform: translateY(-2px);
        }

        .faq-item.active {
            background: rgba(255, 255, 255, 0.03);
            box-shadow: 0 20px 60px -10px rgba(0, 0, 0, 0.6);
            transform: scale(1.02);
        }

        /* 2. O feixe de luz giratório (Fica no fundo) */
        .faq-border-beam {
            position: absolute;
            top: 50%;
            left: 50%;
            width: 250%; /* Bem largo para cobrir os cantos enquanto gira */
            height: 250%;
            /* Gradiente cônico Laranja e Roxo */
            background: conic-gradient(from 0deg at 50% 50%, #FF6600 0deg, #6100B7 45deg, transparent 180deg, transparent 315deg, #FF6600 360deg);
            animation: borderTurn 4s linear infinite;
            opacity: 0; /* Invisível por padrão */
            transition: opacity 0.5s ease;
            z-index: 1;
            pointer-events: none;
        }

        /* Mostra a luz apenas quando o FAQ estiver ativo */
        .faq-item.active .faq-border-beam {
            opacity: 1;
        }

        /* 3. A Máscara (Fica por cima da luz, deixando sobrar só 1px nas bordas) */
        .faq-mask {
            position: absolute;
            inset: 1px; /* É isso que define a espessura da borda brilhante! */
            background: #0A0A0A; /* Mesma cor do fundo do site */
            border-radius: 11px; /* 12px do pai menos o 1px de borda */
            z-index: 2;
            pointer-events: none;
        }

        /* 4. O conteúdo real do FAQ (Fica por cima de tudo) */
        .faq-question {
            position: relative;
            z-index: 5;
            width: 100%;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 32px 40px;
            background: transparent;
            border: none;
            cursor: pointer;
            text-align: left;
            color: #fff;
            outline: none;
        }

        .question-text {
            font-size: 20px;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        .faq-item.active .question-text {
            color: #FF6600; /* Muda a cor do texto para laranja ao abrir */
        }

        .faq-icon {
            font-size: 24px;
            color: #A3A3A3;
            transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .faq-item.active .faq-icon {
            transform: rotate(180deg);
            color: #FF6600;
        }

        .faq-answer {
            position: relative;
            z-index: 5;
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.4s cubic-bezier(0.2, 0.8, 0.2, 1);
        }

        .answer-inner {
            padding: 0 40px 32px 40px;
        }

        .answer-inner p {
            margin: 0;
            font-size: 16px;
            color: #A3A3A3;
            line-height: 1.6;
        }
    </style>
</head>
<body>

    <div class="faq-wrapper">
        <!-- ITEM FAQ -->
        <div class="faq-item">
            <!-- Camadas de Efeito -->
            <div class="faq-border-beam"></div>
            <div class="faq-mask"></div>
            
            <!-- Botão de Pergunta -->
            <button class="faq-question">
                <span class="question-text">Quais ferramentas são utilizadas no curso?</span>
                <iconify-icon icon="solar:alt-arrow-down-linear" class="faq-icon"></iconify-icon>
            </button>
            
            <!-- Resposta -->
            <div class="faq-answer">
                <div class="answer-inner">
                    <p>95% do curso é feito com ferramentas gratuitas de última geração, como o Google Antigravity e o Gemini. Você aprenderá a extrair o máximo poder dessas IAs sem custos adicionais.</p>
                </div>
            </div>
        </div>
        
        <!-- ITEM FAQ 2 -->
        <div class="faq-item">
            <!-- Camadas de Efeito -->
            <div class="faq-border-beam"></div>
            <div class="faq-mask"></div>
            
            <!-- Botão de Pergunta -->
            <button class="faq-question">
                <span class="question-text">Preciso saber programar?</span>
                <iconify-icon icon="solar:alt-arrow-down-linear" class="faq-icon"></iconify-icon>
            </button>
            
            <!-- Resposta -->
            <div class="faq-answer">
                <div class="answer-inner">
                    <p>Absolutamente não. O foco é design imersivo e uso de Agentes de IA. Você aprenderá a criar experiências incríveis sem precisar digitar uma única linha de código.</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Script para abrir/fechar e animar altura -->
    <script>
        document.querySelectorAll('.faq-item').forEach(item => {
            const questionBtn = item.querySelector('.faq-question');
            const answerContainer = item.querySelector('.faq-answer');

            questionBtn.addEventListener('click', () => {
                const isActive = item.classList.contains('active');
                
                // Fecha todos os outros antes de abrir o novo (opcional)
                document.querySelectorAll('.faq-item').forEach(otherItem => {
                    otherItem.classList.remove('active');
                    otherItem.querySelector('.faq-answer').style.maxHeight = '0px';
                });

                // Se não estava ativo, abre. Se estava, fecha.
                if (!isActive) {
                    item.classList.add('active');
                    // Define a max-height dinamicamente baseada no tamanho do conteúdo
                    answerContainer.style.maxHeight = answerContainer.scrollHeight + 'px';
                }
            });
        });
    </script>
</body>
</html>